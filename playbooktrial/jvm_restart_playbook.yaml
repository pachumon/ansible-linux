---
- name: Verify JVM logs and restart service
  hosts: "{{ host }}"
  gather_facts: True

  tasks:
    - name: Include subtask playbook
      import_playbook: verify_file_check_content_playbook.yaml
    
    - name: Run a command
      when: grep_result.rc == 0
      command: echo run command here for jvm restart


  #ansible-playbook ./playbooktrial/jvm_restart_playbook.yaml -e "host=localhost filepath=/home/adminuser/sampledir filename=serverlogs.log errortext=error"