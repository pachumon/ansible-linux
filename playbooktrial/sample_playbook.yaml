---
- name: copy module test
  hosts: linux

  tasks:
    - copy:
        dest: hello
        content: world

    - command:
        cmd: date

- name: install nginx
  hosts: webservers
  tags: nginx
  diff: true
  tasks:
    - yum:
        name: nginx
        state: latest
