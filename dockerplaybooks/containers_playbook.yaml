---

- name: create container to use as ansible managed nodes
  hosts: localhost

  tasks:

  - pip:
      name: requests

  - docker_container:
      name: "{{ item }}"
      image: python
      tty: true
      interactive: true 
    loop:
      - c1
      - c2


  - docker_container:
     name: sleeper
     image: python
     command: sleep infinity 

  



