---

- name: create container to use as ansible managed nodes
  hosts: localhost

  tasks:

  - pip:
      name: requests

  - docker_container:
      name: c1
      image: python
      tty: true
      interactive: true 


  - docker_container:
     name: c2
     image: python
     command: sleep infinity 

  



