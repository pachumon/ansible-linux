---
- hosts: webservers
  become: yes

  vars_files:
    - vars.yaml

  tasks:
    - name: ensure user {{ username }} exists
      user:
        name: "{{ username }}"
        password: "{{ password }}"
        update_password: on_create
