---
- hosts: linux
  become: yes
  gather_facts: false

  tasks:
    #prepare system for enterprise use
    # - name: intall cli web client
    #   yum:
    #     name:
    #       - httpd
    #       - nmap-ncat
    #     state: latest

    - name: check async
      shell: |
        date
        time
        hostname
      async: 3
      poll: 1
      register: async_check

    - name: print async details
      debug:
        msg: async_check
